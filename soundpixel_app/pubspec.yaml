name: soundpixel_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.1
  image_picker: ^1.2.1
  file_picker: ^8.1.2
  audioplayers: ^6.6.0
  cupertino_icons: ^1.0.8
  path_provider: ^2.1.3
  share_plus: ^10.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true